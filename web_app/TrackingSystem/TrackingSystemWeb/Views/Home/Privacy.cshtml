@model List<LocationsViewModel>
<h1>Privacy Policy</h1>

<p>Use this page to detail your site's privacy policy.</p>

@foreach (var item in Model)
{
    <p>Location: @item.Latitude + @item.Longitude + @item.Timestamp</p>
}